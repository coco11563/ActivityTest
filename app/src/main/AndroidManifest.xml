<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pub.sha0w.coco.activitytest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ActivityTest"
            android:launchMode="singleTask"
            android:label="THIS IS MY FIRST ACTIVITY">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".Activity_2">
            <intent-filter>
            <action android:name="pub.sha0w.coco.activitytest.ACTION_START"/>
            <category android:name="android.intent.category.DEFAULT"/>
            <category android:name="pub.sha0w.coco.activitytest.MY_CATEGORY"/>
            </intent-filter>
        </activity>
        <activity android:name=".Activity_3">
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="http" />
            </intent-filter>
        </activity>
    </application>

</manifest>
